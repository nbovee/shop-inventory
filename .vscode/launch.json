{
  "configurations": [
    {
      "name": "Simple Debug",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "python": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/shop-inventory",
            "remoteRoot": "/usr/src/shop-inventory"
          }
        ],
        "projectType": "django"
      }
    }
  ]
}
